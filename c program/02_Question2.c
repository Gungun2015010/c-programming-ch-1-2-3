// qustion number 2!
#include <stdio.h>
int main()
{
    int r = 5;
    float pi = 4.67;
    int height = 5;
    printf("the area or cercle is %f\n", pi * r * r);
    printf("the area of cylander is %f\n", r * r * pi * height);
    return 0;
}